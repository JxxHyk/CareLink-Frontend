/** @type {import('next').NextConfig} */ // ✨ 이 줄을 추가해서 타입 힌트를 줘!
const nextConfig = {
    // reactStrictMode: false, // 필요하다면 주석 해제하여 사용

    // async redirects() { // 필요하다면 주석 해제하여 사용
    //   return [
    //     {
    //       source: "/",
    //       destination: "/login",
    //       permanent: true,
    //     },
    //   ];
    // },

  };

  export default nextConfig;